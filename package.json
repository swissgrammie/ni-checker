{
  "name": "ni-estimator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Should you top up your State Pension? A free triage tool for UK voluntary National Insurance contributions.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test",
    "budget": "node scripts/bundle-budget.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
